import { StoreProvider } from './ui/StoreProvider'
import { createReduxStore } from './config/store'

import { type StateShema, type ReduxStoreWithManager, type thunkExtraAgr } from './config/StateShema'

export { StoreProvider, createReduxStore, type StateShema, type ReduxStoreWithManager, type thunkExtraAgr }
